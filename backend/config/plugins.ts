export default {
  // Включаем Content-Type Builder
  'content-type-builder': {
    enabled: true,
  },

  // Включаем Internationalization (i18n)
  i18n: {
    enabled: true,
  },

  // Включаем Upload plugin (для загрузки изображений и файлов)
  upload: {
    enabled: true,
  },

  // Включаем Users & Permissions (если потребуется управлять доступом)
  'users-permissions': {
    enabled: true,
  },

  // Пример кастомного плагина или конфигурации (если нужно)
  // 'my-plugin': {
  //   enabled: true,
  //   config: {
  //     ...
  //   }
  // }
};